import{_ as i,c as a,o as e,ae as t}from"./chunks/framework.BnYZi0O_.js";const k=JSON.parse('{"title":"从 v1.0.3 升级到 1.1.x","description":"","frontmatter":{},"headers":[],"relativePath":"configuration/upgrade-from-v103.md","filePath":"configuration/upgrade-from-v103.md"}'),n={name:"configuration/upgrade-from-v103.md"};function o(l,s,d,h,p,r){return e(),a("div",null,s[0]||(s[0]=[t(`<h1 id="从-v1-0-3-升级到-1-1-x" tabindex="-1">从 v1.0.3 升级到 1.1.x <a class="header-anchor" href="#从-v1-0-3-升级到-1-1-x" aria-label="Permalink to &quot;从 v1.0.3 升级到 1.1.x&quot;">​</a></h1><p>适用于已经在跑 <strong>xiaozhi-mcphub 1.0.3</strong>（或同代「始终连 PostgreSQL」版本），要升到 <strong>1.1.x</strong> 的用户。<br> 也覆盖：<strong>已经升到 1.1.0–1.1.2 后发现 servers / 端点「丢了」</strong>，但 Postgres volume 还在的场景。</p><h2 id="发生了什么" tabindex="-1">发生了什么 <a class="header-anchor" href="#发生了什么" aria-label="Permalink to &quot;发生了什么&quot;">​</a></h2><table tabindex="0"><thead><tr><th>项目</th><th>v1.0.3</th><th>1.1.x</th></tr></thead><tbody><tr><td>环境变量</td><td><code>DATABASE_URL</code></td><td><strong><code>DB_URL</code></strong>（现已兼容读取旧名）</td></tr><tr><td>MCP 服务器表</td><td><code>mcp_servers</code>（<code>name</code> 主键）</td><td><code>servers</code>（uuid + <code>owner+name</code> 唯一）</td></tr><tr><td>小智端点表</td><td><code>xiaozhi_endpoints</code></td><td>同名（多了 <code>owner</code>）</td></tr><tr><td>无库时</td><td>仍会连默认 localhost Postgres</td><td>可回退到 JSON 文件模式</td></tr></tbody></table><p>1.1.x 早期若只换了镜像、仍只配 <code>DATABASE_URL</code>，应用会当成「没配库」走进文件模式，界面只剩镜像内置示例服务器，并可能在日志里生成新的 admin 密码。<br> 即便连上了同一 Postgres，TypeORM <code>synchronize</code> 也会<strong>新建空的 <code>servers</code> 表</strong>，不会自动把 <code>mcp_servers</code> 拷过去——旧数据其实还在旧表里。</p><h2 id="升级-找回步骤-推荐" tabindex="-1">升级 / 找回步骤（推荐） <a class="header-anchor" href="#升级-找回步骤-推荐" aria-label="Permalink to &quot;升级 / 找回步骤（推荐）&quot;">​</a></h2><ol><li><strong>备份 Postgres volume</strong>（<code>pgdata</code>），不要 <code>docker compose down -v</code>。</li><li>确认 compose / 运行参数指向<strong>同一个</strong>数据库。</li><li>把环境变量改成（或同时保留旧名亦可）：</li></ol><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">environment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  DB_URL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;postgres://xiaozhi:你的密码@db:5432/xiaozhi_mcphub&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # 可选：显式 USE_DB: &quot;true&quot;</span></span></code></pre></div><p>容器内主机名用 compose 服务名（如 <code>db</code>），不要用 <code>127.0.0.1</code>（那是容器自己）。</p><ol start="4"><li>拉起带补偿迁移的版本（含本修复的 1.1.x+）：</li></ol><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 或指定新 tag</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mcphub</span></span></code></pre></div><ol start="5"><li>日志中应出现类似：</li></ol><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[legacy-schema] Found legacy mcp_servers with N row(s); copying any missing servers…</span></span>
<span class="line"><span>  - migrated server: …</span></span>
<span class="line"><span>[legacy-schema] mcp_servers migration done: copied=…, skipped=…</span></span></code></pre></div><ol start="6"><li>登录控制台核对 Servers / 小智端点。 <ul><li>已存在同名 <code>(owner, name)</code> 的新行会被 <strong>skip</strong>（不覆盖你后来手工重建的配置）。</li><li>旧表 <code>mcp_servers</code> <strong>保留作备份</strong>，不会自动 DROP。</li></ul></li></ol><h2 id="已经升到-1-1-2、数据「丢了」怎么办" tabindex="-1">已经升到 1.1.2、数据「丢了」怎么办 <a class="header-anchor" href="#已经升到-1-1-2、数据「丢了」怎么办" aria-label="Permalink to &quot;已经升到 1.1.2、数据「丢了」怎么办&quot;">​</a></h2><p>只要 <strong>Postgres 里还看得到 <code>mcp_servers</code></strong>，升到带本修复的版本再启动一次即可补偿：</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 在库里确认旧表还在</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> COUNT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mcp_servers;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> COUNT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> servers;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> COUNT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> xiaozhi_endpoints;</span></span></code></pre></div><ul><li><code>mcp_servers</code> 有数据、<code>servers</code> 为空或明显偏少 → 启动后会把缺失行拷进 <code>servers</code>。</li><li>小智端点表名未变；连对库后应直接可见，并会把空 <code>owner</code> 回填为 <code>admin</code>。</li></ul><h3 id="若启动报-column-username-contains-null-values" tabindex="-1">若启动报 <code>column &quot;username&quot; … contains null values</code> <a class="header-anchor" href="#若启动报-column-username-contains-null-values" aria-label="Permalink to &quot;若启动报 \`column &quot;username&quot; … contains null values\`&quot;">​</a></h3><p>1.1.3 早期在 TypeORM <code>synchronize</code> 时，可能把 v1.0.3 的无长度 <code>varchar</code> 误重建成 <code>varchar(255) NOT NULL</code>，对已有 admin 行会失败。<br><strong>1.1.4+</strong> 已去掉该 length 漂移，并在 synchronize 前做安全预对齐。</p><p>若你仍卡在 1.1.3，可先手工确认（一般无需改数据——你的 <code>select * from users</code> 里 username 已有值）：</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> username, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">password</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> users;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- username 不应为 NULL</span></span></code></pre></div><p>然后直接拉 <strong>≥1.1.5</strong> 镜像重启即可；不要 <code>DROP</code> users。</p><h3 id="若启动报-column-systemconfig-modelscope-does-not-exist" tabindex="-1">若启动报 <code>column SystemConfig.modelscope does not exist</code> <a class="header-anchor" href="#若启动报-column-systemconfig-modelscope-does-not-exist" aria-label="Permalink to &quot;若启动报 \`column SystemConfig.modelscope does not exist\`&quot;">​</a></h3><p>1.1.4 有一处 bug：把 <code>modelscope</code> 当成「驼峰/蛇形同名」做合并时，把<strong>唯一</strong>的 <code>modelscope</code> 列 DROP 掉了。<br><strong>1.1.5+</strong> 会跳过同名合并，并在启动时 <code>ADD COLUMN IF NOT EXISTS modelscope</code> 自动补回。</p><p>直接拉新镜像重启即可，一般无需手工 SQL。若要手动确认：</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> column_name </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> information_schema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">columns</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> table_name </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;system_config&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ORDER BY</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 应包含 modelscope、smart_routing、mcp_router 等</span></span></code></pre></div><h3 id="admin-密码会不会丢-要不要迁" tabindex="-1">Admin 密码会不会丢？要不要迁？ <a class="header-anchor" href="#admin-密码会不会丢-要不要迁" aria-label="Permalink to &quot;Admin 密码会不会丢？要不要迁？&quot;">​</a></h3><p><strong>不用单独迁 admin 密码。</strong> v1.0.3 的账号就在同一 Postgres 的 <strong><code>users</code></strong> 表里（bcrypt 哈希，表名未改）。</p><table tabindex="0"><thead><tr><th>情况</th><th>登录用什么</th></tr></thead><tbody><tr><td>连回<strong>原来的</strong> <code>pgdata</code> / 同一库</td><td><strong>你升级前自己设过的 admin 密码</strong>（或 v1.0.3 默认 <code>admin123</code>，若从未改过）</td></tr><tr><td>1.1.x 曾因没配 <code>DB_URL</code> 走进文件模式，日志打印了随机密码</td><td>那是写在<strong>空的 JSON 用户列表</strong>里的一次性账号；<strong>不会</strong>写回覆盖 Postgres 里的旧 admin</td></tr><tr><td>生产环境空库首次启动</td><td>才会生成随机密码并打日志（<code>admin123</code> 仅 development）</td></tr></tbody></table><p>启动成功连上旧库时日志类似：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[legacy-schema] Reusing N existing user(s) from the database (including admin). Keep the password you set before the upgrade…</span></span>
<span class="line"><span>User store already has N account(s); reusing existing admin credentials (not regenerating password)</span></span></code></pre></div><p>可在库里自检：</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> username, is_admin, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">password</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hash_len, created_at</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> users</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ORDER BY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> created_at;</span></span></code></pre></div><p><code>admin</code> 行还在且 <code>hash_len</code> 正常（bcrypt 一般 60）→ 用<strong>旧密码</strong>登录即可，无需 reset。<br> 只有 <code>users</code> 也空了（例如误删 volume）才需要用日志里的新密码或 <code>ADMIN_PASSWORD</code> 重建。</p><p>若 <code>mcp_servers</code> 和 volume 都没了，应用无法凭空恢复，只能从外部备份还原。</p><h2 id="兼容说明" tabindex="-1">兼容说明 <a class="header-anchor" href="#兼容说明" aria-label="Permalink to &quot;兼容说明&quot;">​</a></h2><ul><li><strong><code>DATABASE_URL</code></strong>：启动时仍可读，并打一次 deprecation 警告；请尽快改成 <code>DB_URL</code>。</li><li><strong>空 DB URL + 智能路由 embedding</strong>：不再默默连 <code>127.0.0.1:5432</code>，会跳过或明确报错。</li><li><strong>JSON 文件 → DB</strong>：仅在新库 <code>users</code> 为空时从 <code>mcp_settings.json</code> 导入；<strong>不能</strong>替代 <code>mcp_servers</code> 补偿（那是另一条路径）。</li></ul><h2 id="相关文件" tabindex="-1">相关文件 <a class="header-anchor" href="#相关文件" aria-label="Permalink to &quot;相关文件&quot;">​</a></h2><ul><li><code>src/config/dbEnv.ts</code> — <code>DB_URL</code> / <code>DATABASE_URL</code> 解析</li><li><code>src/utils/legacySchemaMigration.ts</code> — <code>mcp_servers</code> → <code>servers</code> 等</li><li><code>src/utils/migration.ts</code> — 启动时调用补偿 + owner 回填</li></ul>`,40)]))}const g=i(n,[["render",o]]);export{k as __pageData,g as default};
